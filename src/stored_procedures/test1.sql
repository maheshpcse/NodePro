CREATE PROCEDURE `test_user`(IN `id` INT(100)) NOT DETERMINISTIC NO SQL SQL SECURITY DEFINER BEGIN CREATE TEMPORARY TABLE IF NOT EXISTS users( `name` VARCHAR(50) NOT NULL, `age` INT(50) NOT NULL ) ENGINE=memory; set @mquery = concat("insert into users (name,age) values ('mahesh',23)"); PREPARE stat FROM @mquery; EXECUTE stat; set @mquery1 = concat("select * from users"); PREPARE stat FROM @mquery1; EXECUTE stat; END